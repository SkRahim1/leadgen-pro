/**
 * @module lib/auth/options
 * @description NextAuth configuration — STUBBED for demo mode.
 * Uncomment and install next-auth when real Google OAuth keys are available.
 *
 * Install: npm install next-auth @auth/prisma-adapter
 */

// import type { NextAuthOptions } from "next-auth"
// import GoogleProvider from "next-auth/providers/google"
// import { PrismaAdapter } from "@auth/prisma-adapter"
// import { prisma } from "@/lib/db/prisma"

/** Placeholder for demo mode — replace with real NextAuth config when keys are ready */
export const authOptions = {} as any
